# Database Configuration (Neon PostgreSQL)
# Optimized for high-latency environments and Neon cold-starts
spring.datasource.url=jdbc:postgresql://ep-calm-salad-amptoey4-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&prepareThreshold=0&tcpKeepAlive=true&socketTimeout=600&connectTimeout=300&loginTimeout=300&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_geuU41yzsKEZ
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Pool Configuration (Throttled for stability over high-latency Neon connection)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.connection-timeout=120000
spring.datasource.hikari.keepalive-time=60000
spring.datasource.hikari.leak-detection-threshold=2000
spring.datasource.hikari.initialization-fail-timeout=0

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# Explicitly set dialect and disable metadata fetch to avoid timeout on cold starts
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
app.jwt.secret=9a4f4e3545634b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b4b
app.jwt.expiration=3600000
app.jwt.refreshExpiration=604800000

# Server Configuration
server.port=8080
server.error.include-message=always

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:diutaj0s9}
cloudinary.api-key=${CLOUDINARY_API_KEY:792391589758571}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:rnFRLM-N7g41CKjg_kiFjQLhq74}

# File Upload Limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# Jackson Date Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
